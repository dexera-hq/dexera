# Environment Model

Deployment tiers:

- `local`: developer machines
- `dev`: shared dev environment (auto-deploy from `main`)
- `staging`: production-like smoke environment
- `prod`: production

All examples are non-secret templates only.
